# CLAUDE.md

Repo-level instructions for Claude Code working in `gpu-inference-lab`.

## Agent skills

### Issue tracker

Issues live in GitHub Issues at `tungsheng/gpu-inference-lab`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical names (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`) — created on first use. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout — one `CONTEXT.md` + `docs/adr/` at the repo root (created lazily by `/grill-with-docs`). See `docs/agents/domain.md`.
