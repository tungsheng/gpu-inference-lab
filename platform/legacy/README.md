# platform/legacy

This directory keeps manifests that are no longer part of the active platform
path but may still need cleanup support in `./scripts/down`.

Do not apply these manifests for new workflows.
