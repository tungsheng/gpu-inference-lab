# platform/ingress

This directory is reserved for reusable ingress resources as the repository moves from the current sample app layout toward the target roadmap structure.

Current ingress manifests still live under `platform/test-app/`.
