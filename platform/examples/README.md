# platform/examples

This directory contains optional sample workloads that are not part of the
default platform lifecycle.

- `echo/`: a tiny HTTP echo app for manual ingress experiments

The scripted workflows intentionally use the manifests under `platform/` that
match their purpose directly, such as inference, observability, Karpenter, and
runtime validation workloads.
